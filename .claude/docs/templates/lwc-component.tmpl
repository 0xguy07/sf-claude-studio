// Template: LWC component skeleton
// Files: <component>.js, <component>.html, <component>.css, <component>.js-meta.xml,
//        __tests__/<component>.test.js
// See rules/lwc-code.md for what's mandatory.

// ───── <component>.js ─────
import { LightningElement, api, wire } from 'lwc';
// import getThing from '@salesforce/apex/<Controller>.getThing';

export default class <ComponentPascalCase> extends LightningElement {
    @api recordId;

    // @wire(getThing, { id: '$recordId' }) thing;

    handleClick(event) {
        // template.querySelector — never document.querySelector
        // const el = this.template.querySelector('lightning-input');
    }
}

// ───── <component>.html ─────
// <template>
//   <lightning-card title="{title}">
//     <p slot="actions">…</p>
//   </lightning-card>
// </template>

// ───── <component>.js-meta.xml ─────
// <?xml version="1.0" encoding="UTF-8"?>
// <LightningComponentBundle xmlns="http://soap.sforce.com/2006/04/metadata">
//     <apiVersion>62.0</apiVersion>
//     <isExposed>true</isExposed>
//     <targets>
//         <target>lightning__RecordPage</target>
//     </targets>
// </LightningComponentBundle>

// ───── __tests__/<component>.test.js ─────
// import { createElement } from 'lwc';
// import <ComponentPascalCase> from 'c/<componentCamelCase>';
//
// describe('c-<component-kebab-case>', () => {
//   afterEach(() => {
//     while (document.body.firstChild) document.body.removeChild(document.body.firstChild);
//   });
//
//   it('renders with a recordId', () => {
//     const el = createElement('c-<component-kebab-case>', { is: <ComponentPascalCase> });
//     el.recordId = '001000000000001';
//     document.body.appendChild(el);
//     expect(el.shadowRoot.querySelector('lightning-card')).not.toBeNull();
//   });
// });
