# Claude Code session state (per-developer, not shared)
.claude/agent-memory/
.claude/sessions/
.claude/active.md

# Salesforce DX
.sfdx/
.sf/
.localdevserver/
*.log

# Local MCP config (template ships .mcp.json.example; copy to .mcp.json locally)
.mcp.json

# Internal marketing / pitch drafts — not for public repo
marketing/

# Node / LWC tooling
node_modules/
coverage/
.nyc_output/
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# CLI build artifacts (regenerated by `npm run build:templates`)
cli/node_modules/
cli/templates/

# Studio install manifest is per-project, not for the source repo
.studio-manifest.json
